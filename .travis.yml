language: cpp

compiler:
  - gcc

os: linux

before_script:
  - mkdir build
  - cd build
  - cmake -D CMAKE_CXX_COMPILER=g++-10 -D CMAKE_C_COMPILER=gcc-10 ..

script:
  - cmake --build . --config Release